<template>
  <router-link
    class="event-link"
    :to="{ name: 'EventDetails', params: { id: event.id } }"
  >
    <div class="event-card">
      <span>A las {{ event.time }} en {{ event.date }}</span>
      <h4>Categoría:</h4><span>{{ event.category }}</span>
      <h3>{{ event.title }}</h3>
    </div>
  </router-link>
</template>

<script>
export default {
  props: {
    event: {
      type: Object,
      required: true
    }
  }
}
</script>

<style scoped>
.event-card {
  border: 1px solid #39495c;
  cursor: pointer;
  margin-bottom: 18px;
  padding: 20px;
  width: 250px;
}
.event-card:hover {
  transform: scale(1.01);
  box-shadow: 0 3px 12px 0 rgba(0, 0, 0, 0.2);
}
.event-link {
  color: #2c3e50;
  text-decoration: none;
}
</style>
